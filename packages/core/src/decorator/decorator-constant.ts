/**
 * 注入的key
 */
export const INJECT_KEY = Symbol('INJECT-KEY');

/**
 * 是否可以被注入的key
 */
export const INJECTABLE_KEY = Symbol('INJECTABLE-KEY');

/**
 * 参数可选的key
 */
export const OPTIONAL_KEY = Symbol('OPTIONAL_KEY');
